import { Component } from '@angular/core';

@Component({
    selector: 'header',
    templateUrl: './header.component.html',
    styles: ['h1 {color: red}'],
})

export class HeaderComponent {
    // headerIcon = 
};